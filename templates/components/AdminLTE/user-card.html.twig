<li class="dropdown user user-menu">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
      <img src="{{ current_user.avatar }}" class="user-image" alt="User Image">
      <span class="hidden-xs">{{current_user.user_name}}</span>
    </a>
    <ul class="dropdown-menu">
      <!-- User image -->
      <li class="user-header">
        <img src="{{ current_user.avatar }}" class="img-circle" alt="User Image">

        <p>
          {{current_user.user_name}}
          <!-- <small>Member since Nov. 2012</small> -->
        </p>
      </li>
      <!-- Menu Body -->
{#
      <li class="user-body">
        <div class="row">
          <div class="col-xs-4 text-center">
            <a href="#">Followers</a>
          </div>
          <div class="col-xs-4 text-center">
            <a href="#">Sales</a>
          </div>
          <div class="col-xs-4 text-center">
            <a href="#">Friends</a>
          </div>
        </div>
        <!-- /.row -->
      </li>
#}
      <!-- Menu Footer-->
      <li class="user-footer">
        <div class="pull-left">
          <a href="{{site.uri.public}}/account/settings" class="btn btn-default btn-flat">Settings</a>
        </div>
        <div class="pull-right">
          <a href="{{site.uri.public}}/account/logout" class="btn btn-default btn-flat">Sign out</a>
        </div>
      </li>
    </ul>
</li>